services:
  app:
    image: "inventory-manager"
    container_name: "inventory-manager-app"
    build: .
    init: true
    shm_size: "512mb"
    ports:
      - "3000:3000"
    env_file: .env
    restart: unless-stopped